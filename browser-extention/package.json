{
    "name": "security-training-extension",
    "version": "1.0.0",
    "scripts": {
      "build": "tsc && npm run copy-files",
      "copy-files": "cp src/manifest.json dist/ && cp src/popup.html dist/",
      "watch": "tsc -w"
    },
    "devDependencies": {
      "@types/chrome": "^0.0.254",
      "typescript": "^5.0.0"
    }
  }